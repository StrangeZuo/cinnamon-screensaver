From: Margarita Manterola <marga@debian.org>
Date: Thu, 8 Dec 2016 16:23:20 -0300
Subject: Switch to the Debian path for iso flags

Forwarded: not-needed
---
 src/passwordEntry.py | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/src/passwordEntry.py b/src/passwordEntry.py
index 42c15f4..b282dfe 100644
--- a/src/passwordEntry.py
+++ b/src/passwordEntry.py
@@ -76,7 +76,7 @@ class PasswordEntry(Gtk.Entry):
             ui_scale = self.get_scale_factor()
 
             if name:
-                filename = "/usr/share/iso-flag-png/%s.png" % name
+                filename = "/usr/share/iso-flags-png-320x240/%s.png" % name
 
                 try:
                     pixbuf = GdkPixbuf.Pixbuf.new_from_file_at_size(filename, -1, height * ui_scale)
